import { GetCodeLink } from '../../../components/GetCodeLink';

# JavaScript で Hello World

今回のソースコードは<GetCodeLink>こちら</GetCodeLink>からダウンロードできます。

まずは、JavaScript で Hello World を表示してみましょう。

```js filename="index.js"
console.log('Hello World');
```

{/* [こちら](https://onecompiler.com/javascript)のサイトを利用してコードを実行します。<br/> */}
実行結果は以下のようになります。

```bash
Hello World!
```

次は好きな文字列を表示してみましょう。

```js filename="index.js"
console.log('こんにちは');
```

実行結果は以下のようになります。

```bash
こんにちは
```

このように、JavaScript では`console.log()`を使って文字列を表示することができます。
