import { GetCodeLink } from '../../../components/GetCodeLink';

# Python で Hello World

今回のソースコードは<GetCodeLink>こちら</GetCodeLink>からダウンロードできます。

まずは、Python で Hello World を表示してみましょう。

```js filename="index.py"
print('Hello World!');
```

{/* [こちら](https://onecompiler.com/python)のサイトを利用してコードを実行します。<br/> */}

以下のようにコードを入力し、実行ボタンを押してください。

```bash
python src/index.py
```

実行結果は以下のようになります。

```bash
Hello World!
```

次は好きな文字列を表示してみましょう。

```js filename="index.py"
print('こんにちは');
```

実行結果は以下のようになります。

```bash
こんにちは
```

このように、Python では`print`関数を使って文字列を表示することができます。
