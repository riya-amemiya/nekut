import { Tab, Tabs } from 'nextra-theme-docs';

# C 言語の環境構築

多くの OS では、C 言語のコンパイラが標準でインストールされています。
下記のコマンドを実行して、C 言語のコンパイラがインストールされているか確認してみましょう。

```bash
gcc --version
```

gcc がインストールされていない場合は、下記のコマンドでインストールしてください。

<Tabs items={['macOS', 'Windows']}>

<Tab>
    インストールされてない場合、`gcc`コマンドを実行すると、ウィンドウが表示されるので、その画面に従ってインストールしてください。
</Tab>

<Tab>準備中</Tab>

</Tabs>
